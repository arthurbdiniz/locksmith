<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @document.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @document.description %>
</p>

<p>
<strong>Link:</strong>
<%= link_to("gpg", rails_blob_path(@document.file, disposition: "attachment")) %>
</p>

<%= text_area(:comment, :text, size: "20x30", id: 'mytext') %>

<%= link_to 'Edit', edit_document_path(@document) %> |
<%= link_to 'Back', documents_path %>
